global:
  scrape_interval: 10s
scrape_configs:
  - job_name: prometheus
    static_configs: [{ targets: ["prometheus:9090"] }]
  - job_name: finanalytics-backend
    metrics_path: /metrics
    static_configs: [{ targets: ["backend:8000"] }]
